//const fs = require('fs');
//const input = fs.readFileSync('sample.txt').toString().split("\n");
//const titles = [];

// function capitalizeFirstLetter(string) {
//   return string.charAt(0).toUpperCase() + string.slice(1);
// }

// for (let i of input) {
//   const tokens = i.split('-');
//   const transfomed = tokens.map((t) => capitalizeFirstLetter(t)).join(' ');
//   titles.push(transfomed);
// }
//  JSON.stringify({ titles: titles }, null, 2)
//fs.writeFileSync('titles.json', 'hello world');